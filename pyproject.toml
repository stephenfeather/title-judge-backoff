[project]
name = "title-judge-bakeoff"
version = "0.1.0"
description = "LLM judge bake-off harness for product-title enrichment QA"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
